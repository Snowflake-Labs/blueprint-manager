-- ============================================================================
-- SCHEMA ORGANIZATION PLANNING
-- ============================================================================
-- Data Product: {{ data_product_name }}
-- Domain: {{ data_product_domain }}
-- Environment: {{ data_product_environment }}
{%- if account_strategy != 'Single Account' %}
-- Target Account: {{ target_account_name | upper }}
{%- endif %}
-- ============================================================================

/*
SCHEMA ORGANIZATION DEFINED
===========================

This step captures schema definitions. Schemas will be created in the Database & Schema Setup task.

Schemas by Zone:
{%- for zone in zone_list %}

{{ zone | upper }} Zone:
{%- for schema in schema_definitions if schema.zone == zone %}
  - {{ schema.schema_name }}
    Purpose: {{ schema.purpose }}
    Classification: {{ schema.data_classification }}
{%- endfor %}
{%- endfor %}

Total Schemas: {{ schema_definitions | length }}

All schemas will be created WITH MANAGED ACCESS for centralized privilege management.
*/