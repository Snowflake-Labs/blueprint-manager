-- ============================================================================
-- CONFIGURE PARAMETERS - Python / nipyapi STEP
-- ============================================================================
-- Profile:   {{ nipyapi_profile }}
-- Jira URL:  {{ jira_url }}
-- Jira User: {{ jira_email }}
-- Project:   {{ jira_project_keys }}
-- Dest:      {{ dest_db }}.{{ dest_schema }}.{{ dest_table }}
-- Warehouse: {{ warehouse_name }}
-- Role:      {{ runtime_role }}
-- ============================================================================

-- This step is completed via a Python script. No SQL is executed here.

/*
PARAMETER CONTEXT CONFIGURATION
================================
Jira URL:        {{ jira_url }}
Jira Email:      {{ jira_email }}
Project Key(s):  {{ jira_project_keys }}
Destination:     {{ dest_db | upper }}.{{ dest_schema | upper }}.{{ dest_table | upper }}
Warehouse:       {{ warehouse_name | upper }}
Runtime Role:    {{ runtime_role | upper }}
Auth Strategy:   SNOWFLAKE_MANAGED

NOTE: The Jira API Token is NOT stored in this answers file.
      Pass it securely via --jira-token at script execution time.
*/
