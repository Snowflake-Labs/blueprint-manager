## Create External Access Integration

### Configuration

| Setting | Value |
|---------|-------|
| **EAI Name** | `JIRA_EAI` |
| **Network Rule** | `{{ rule_db | upper }}.{{ rule_schema | upper }}.JIRA_NETWORK_RULE` |
| **Enabled** | TRUE |

### Next Step

After creating the EAI, you must attach it to your Openflow runtime via the Control Plane UI (step 7). The EAI will not take effect until it is associated with the runtime.
