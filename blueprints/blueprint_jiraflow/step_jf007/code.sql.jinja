-- ============================================================================
-- ATTACH EAI TO RUNTIME - UI STEP
-- ============================================================================
-- EAI: JIRA_EAI
-- ============================================================================

-- This step is completed in the Openflow Control Plane UI.
-- No SQL is executed here.

/*
UI STEPS
========
1. Open the Openflow Control Plane UI
2. Navigate to your runtime
3. Open Settings -> External Access Integrations
4. Add JIRA_EAI
5. Save and allow the runtime to restart if prompted

The runtime must restart to pick up the new EAI. Wait for it to return
to a running state before proceeding with connector deployment.
*/
