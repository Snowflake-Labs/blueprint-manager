-- ============================================================================
-- ZONE STRUCTURE PLANNING
-- ============================================================================
-- Data Product: {{ data_product_name }}
-- Domain: {{ data_product_domain }}
-- Environment: {{ data_product_environment }}
{%- if account_strategy != 'Single Account' %}
-- Target Account: {{ target_account_name | upper }}
{%- endif %}
-- ============================================================================

/*
ZONE STRUCTURE DEFINED
======================

This step captures the zone structure. Databases will be created in the Database & Schema Setup task.

Zones Configured ({{ zone_list | length }} total):
{%- for zone in zone_list %}
- {{ zone }}
{%- endfor %}

Database Names (components excluded when at account level):
{%- for zone in zone_list %}
{%- if account_strategy == 'Single Account' %}
- {{ data_product_domain | upper }}_{{ data_product_name | upper }}_{{ zone | upper }}_{{ data_product_environment | upper }}
{%- elif account_strategy == 'Multi-Account (Environment-based)' %}
- {{ data_product_domain | upper }}_{{ data_product_name | upper }}_{{ zone | upper }}
{%- elif account_strategy == 'Multi-Account (Domain-based)' %}
- {{ data_product_name | upper }}_{{ zone | upper }}_{{ data_product_environment | upper }}
{%- elif account_strategy == 'Multi-Account (Domain + Environment)' %}
- {{ data_product_name | upper }}_{{ zone | upper }}
{%- endif %}
{%- endfor %}

Access Control:
Access is controlled through the role hierarchy (Role & Access Control task):
- ADMIN/CREATE: All zones
- WRITE: Curated and consumption zones
- READ: Consumption zone only
*/