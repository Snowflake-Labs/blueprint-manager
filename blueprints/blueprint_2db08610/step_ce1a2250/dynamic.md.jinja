## Notification Configuration

### Alert Recipients

Notifications will be sent to:

{%- if resource_monitor_emails and resource_monitor_emails | length > 0 %}
{%- for email in resource_monitor_emails %}
- `{{ email }}`
{%- endfor %}
{%- else %}
- Account administrators (default)
{%- endif %}

### Alert Triggers

Notifications will be sent when these thresholds are crossed:

| Threshold | Credits | Action |
|-----------|---------|--------|
| 75% | {{ (resource_monitor_limit | int * 0.75) | int }} | Notify |
| 90% | {{ (resource_monitor_limit | int * 0.90) | int }} | Notify |
| 100% | {{ resource_monitor_limit }} | {{ resource_monitor_action }} |
| 110% | {{ (resource_monitor_limit | int * 1.10) | int }} | {{ resource_monitor_action }} |