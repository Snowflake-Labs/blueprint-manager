## Additional Warehouse Tags

### Already Applied (Create Warehouses step)

These core tags were applied when warehouses were created:

| Tag | Value |
|-----|-------|
| `DOMAIN` | {{ data_product_domain | upper }} |
| `ENVIRONMENT` | {{ data_product_environment | upper }} |
| `DATAPRODUCT` | {{ data_product_name | upper }} |
| `WORKLOAD` | (varies per warehouse) |

### Additional Tags (This Step)

{%- if cost_center and cost_center != 'NONE' %}

The following additional tag will be applied:

| Tag | Value |
|-----|-------|
| `COST_CENTER` | {{ cost_center | upper }} |

### Warehouses to Update

{%- for wh in warehouse_definitions %}
{%- if object_component_order == '<domain>_<env>_<dataproduct>' %}
{%- if account_strategy == 'Single Account' %}- `{{ data_product_domain | upper }}_{{ data_product_environment | upper }}_{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- elif account_strategy == 'Multi-Account (Environment-based)' %}- `{{ data_product_domain | upper }}_{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- elif account_strategy == 'Multi-Account (Domain-based)' %}- `{{ data_product_environment | upper }}_{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- elif account_strategy == 'Multi-Account (Domain + Environment)' %}- `{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- endif %}
{%- elif object_component_order == '<domain>_<dataproduct>_<env>' %}
{%- if account_strategy == 'Single Account' %}- `{{ data_product_domain | upper }}_{{ data_product_name | upper }}_{{ data_product_environment | upper }}_{{ wh.warehouse_name | upper }}`{%- elif account_strategy == 'Multi-Account (Environment-based)' %}- `{{ data_product_domain | upper }}_{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- elif account_strategy == 'Multi-Account (Domain-based)' %}- `{{ data_product_name | upper }}_{{ data_product_environment | upper }}_{{ wh.warehouse_name | upper }}`{%- elif account_strategy == 'Multi-Account (Domain + Environment)' %}- `{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- endif %}
{%- elif object_component_order == '<env>_<domain>_<dataproduct>' %}
{%- if account_strategy == 'Single Account' %}- `{{ data_product_environment | upper }}_{{ data_product_domain | upper }}_{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- elif account_strategy == 'Multi-Account (Environment-based)' %}- `{{ data_product_domain | upper }}_{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- elif account_strategy == 'Multi-Account (Domain-based)' %}- `{{ data_product_environment | upper }}_{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- elif account_strategy == 'Multi-Account (Domain + Environment)' %}- `{{ data_product_name | upper }}_{{ wh.warehouse_name | upper }}`{%- endif %}
{%- endif %}
{%- endfor %}

{%- else %}

**No additional tags to apply.** Cost center was not specified.

You can skip this step or add a cost center later by running:
```sql
ALTER WAREHOUSE <warehouse_name> SET TAG
  {{ platform_database_name | upper }}{% if account_strategy != 'Single Account' %}_SHARED{% endif %}.{{ governance_name | upper }}.COST_CENTER = '<your_code>';
```

{%- endif %}