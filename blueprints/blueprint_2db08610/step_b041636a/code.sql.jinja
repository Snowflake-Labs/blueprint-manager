-- ============================================================================
-- DATA PRODUCT IDENTITY
-- ============================================================================
-- Data Product: {{ data_product_name }}
-- Domain: {{ data_product_domain }}
-- Environment: {{ data_product_environment }}
-- Account Strategy: {{ account_strategy }}
{%- if account_strategy != 'Single Account' %}
-- Target Account: {{ target_account_name | upper }}
{%- endif %}
-- ============================================================================
-- EXECUTE FROM: {% if account_strategy != 'Single Account' %}{{ target_account_name | upper }}{% else %}Primary Account{% endif %}
-- ============================================================================

/*
DATA PRODUCT IDENTITY DEFINED
=============================

This step captures the identity of your data product. No SQL is executed here.

Data Product Details:
- Name: {{ data_product_name }}
- Domain: {{ data_product_domain }}
- Environment: {{ data_product_environment }}
- Description: {{ data_product_description }}

Account Strategy: {{ account_strategy }}
{%- if account_strategy == 'Single Account' %}
- Object Naming: Full pattern (domain + dataproduct + zone + env)
{%- elif account_strategy == 'Multi-Account (Environment-based)' %}
- Object Naming: Excludes environment (it's at account level)
{%- elif account_strategy == 'Multi-Account (Domain-based)' %}
- Object Naming: Excludes domain (it's at account level)
{%- elif account_strategy == 'Multi-Account (Domain + Environment)' %}
- Object Naming: Excludes domain and environment (both at account level)
{%- endif %}

Cost Allocation Tags (applied regardless of naming):
- DOMAIN = '{{ data_product_domain | upper }}'
- ENVIRONMENT = '{{ data_product_environment | upper }}'
- DATAPRODUCT = '{{ data_product_name | upper }}'

These values will be used in subsequent steps to:
1. Generate database and schema names (based on account strategy)
2. Create roles with appropriate naming
3. Apply cost allocation tags
4. Configure resource monitors
*/